https://team.t1.ru/wiki/3a11d94c-3a75-c0d7-ee88-e0695366d3de
Java базовый курс
https://basecode.t1.ru/course_sessions/723
https://admin-lms.t1.ru/viewer/sessions/723
Савушкина Юлия Игоревна <YSavushkina@inno.tech>


Результат для бизнеса
Участники узнают, как работают инструменты сборки, обрабатываются исключения и работает память Java-приложения;
смогут разрабатывать и проверять свой код unit-тестами
Целевая аудитория
Аналитики, тестировщики, инженеры сопровождения, разработчики, CI/CD-инженеры

Слушатели научатся:
разрабатывать простые программы на Java,
использовать IntelliJ Idea для разработки ПО,
пользоваться системами версионирования, 
компилировать и запускать Java, Classpath, JVM, JRE, JDK,
считать тестовое покрытие,
работать с Collections, Stream API, Reflection API,
писать unit-тесты, используя JUnit5,
сравнивать объекты.
Формат
Гибридный: предзаписанная теория, практические задания, синхронные встречи с экспертами

Требуемое ПО
Intelij IDEA, Maven, Java, git
Продолжительность и документ
30 часов

сертификат
Необходимый уровень подготовки

Требует базового понимания ИТ
 

1. Java Core	
Java Basic
Введение в ООП
Collections
Stream API,
Reflection API
Память Java-приложения

2. Общие знания	
GIT и среда разработки IntelliJ Idea
Инструменты сборки
TCP/IP и протокол HTTP
Архитектура Web-проекта

3. Общая теория тестирования	Тестирование


========================== Java Basic:
========================== Java Basic. История, особенности, компиляция, JRE, JDK, JAVAC, JAR:
-------------------------- история:
Разработка первой работающей версии заняла 18 месяцев. Вначале язык получил название «OAK» (Дуб), но в 1995 г. он был переименован в «Java».
ОаК, Green		08.1991		модификация языка C++ в сторону упрощения и снижения порога вхождения:
			надёжность и механизмы безопасности	-позволяет разработчику не обращать на эти вопросы внимание;
			работа с разными типами данных		-ПК, мобил, ЧИПы банковских карт;
			Объектная ориентация			-проще разрабатывать soft;
			Объекты, доступные по сети		-встроенные инструменты работы в сети.
Основной причиной была потребность в независящем от платформы (т.е. архитектурно нейтральном) языке, который можно было бы использовать для создания программного обеспечения, встраиваемого в различные бытовые электронные устройства, такие как СВЧ-печи и устройства дистанционного управления. На тот момент уже был популярен язык С (Си), но проблема применения языков С и С++ (как и большинства других языков) состоит в том, что написанные на них программы должны компилироваться для конкретной платформы. Хотя программы С++ могут быть скомпилированы практически для любого типа процессора, для этого требуется наличие полного компилятора С++, предназначенного для данного процессора. Проблема в том, что создание компиляторов обходится дорого и требует значительного времени. Поэтому требовалось более простое — и экономически выгодное — решение. Гослинг и другие начали работу над переносимым, не зависящим от платформы языком, который можно было бы использовать для создания кода, пригодного для выполнения на различных процессорах в различных средах. Вскоре эти усилия привели к созданию языка Java. К 1993 г. членам группы проектирования Java стало очевидным, что проблемы переносимости, часто возникающие при создании кода, предназначенного для встраивания в контроллеры, возникают также и при попытках создания кода для Internet. Фактически, та же проблема, для решения которой в малом масштабе предназначался язык Java, в большем масштабе была актуальна и в среде Internet. Понимание этого обстоятельства вынудило разработчиков языка Java перенести свое внимание с бытовой электроники на программирование для Internet. Это стало «начальной искрой», Internet обеспечил крупномасштабный успех Java. В какой-то момент возникла идея сделать универсальную платформу, на которой могли бы работать приложения, написанные под разные ОС с разным аппаратным обеспечением. Для реализации этой задумки была создана виртуальная машина (Java Virtual Machine, JVM). Это специальное приложение, которое позволяет запускать программы, написанные на Java, на любой ОС и любом железе (при условии, что JVM реализована для используемой платформы). Виртуальная машина является прослойкой между процессором и программным кодом. При этом она запускает не файлы с исходным кодом (с расширением .java), а специальные промежуточные файлы с расширением .class в которых хранится байт-код. Этот код назван так из-за того, что представляет собой набор команд в один байт, хотя имеются команды и большей длины. Чтобы из исходного файла получить файл, понятный JVM, его нужно скомпилировать. 
Java			компилируемый язык, поэтому для ...
Java 			порядок объявления кода в файле не имеет значения, порядок выполнения != последовательнсоти кода.
-------------------------- Особенности Java:
Различные аппаратные системы					Intelx86, SunSPARC, PowerPC
Различные программные системы					Microsoft, Sun, Linux, MacOS
Одинаковый функционал на различных платформах			на любой программно-аппаратной платформе
Java Virtual Machine						JVM 1996-1999 - обеспечивает универсальность. Виртуальная машина является прослойкой между процессором и программным кодом. При этом она запускает не файлы с исходным кодом (с расширением .java), а специальные промежуточные файлы с расширением .class в которых хранится байт-код. Этот код назван так из-за того, что представляет собой набор команд в один байт, хотя имеются команды и большей длины. Чтобы из исходного файла получить  файл, понятный JVM, его нужно скомпилировать.
строгая типизация						отсутствие возможности изменения типа данных, в процессе работы приложения
Кросс-платформенность
Объектно-ориентированность
Встроенный модуль безопасности					механизмы работы с памятью
Ориентация на интернет-задачи, распределённые приложения	акцент на целевые области
Динамичность, легкость развития					))) по крайней мере закладывалась, как особенность, при разработке языка
Лёгкость в освоении						проще в освоении, чем с++
-------------------------- Компиляция и использование приложения:
Компиляция —		это процесс преобразования исходного кода, написанного на языке программирования, например, Java в специальный код, который компьютер (в нашем случае JVM) может понять и запустить. Для компиляции используется программа под названием «компилятор». Все виртуальные машины, неважно на какой ОС они установлены, знают и понимают байт-код одинаково хорошо, т. к. он представляет собой не машинные команды конкретной компьютерной платформы (конкретного процессора в виде последовательности нулей и единиц), а команды JVM. За счет этого и реализуется кроссплатформенность. Промежуточный код не зависит от какой-то ОС или процессора, т.
к. не содержит код, созданный специально под них. Он содержит байт-код, который одинаково интерпретируется JVM на любой платформе.
-Исходный код		.java
-через "Компилятор"			Java Compiler. Компилирует исходный код *.java в байт-код *.class;
-Байт-код		.class		позволяет коду быть неизменным. https://ru.wikipedia.org/wiki/%D0%91%D0%B0%D0%B9%D1%82-%D0%BA%D0%BE%D0%B4 Использование
одного байт-кода для многих платформ позволяет описать Java как «СКОМПИЛИРОВАНО ОДНАЖДЫ, ЗАПУСКАЕТСЯ ВЕЗДЕ» (COMPILE ONCE, RUN ANYWHERE).
-через "JVM"				переводит в бинарный код, который выполняется аппаратной системой. Берёт на себя обязательство одникового исполнения Байт-кода на различный аппаратный платформах.
-Исполняемый код
-------------------------- Поставки Java:
Java Runtime Environment 		JRE	виртуальная машина и стандартная библиотека классов, для запуска приложения. Среда исполнения Байт-кода.
Java Development Kit			JDK	набор инструментов для разработчика, включает в себя JRE, компилятор и прочее. Для создания программ на Java
существует специальный набор инструментов для разработчиков, называемый Java Development Kit (JDK). Из этого набора нам сегодня потребуются две утилиты (самые основные и важные) под названием javac (сокр. от java compiler) и java (да, называется так же, как и язык!). Существует множество сайтов, предлагающих загрузку JDK, и неясно, «кто что предлагает и с каким лицензированием». С точки зрения исходного кода вашего JRE/JDK есть только один http://openjdk.java.net/projects/jdk/ .Однако это всего лишь исходный код, а не распространяемая сборка (файл .zip с скомпилированной командой java для вашей конкретной операционной системы). Теоретически вы и я могли бы создать сборку из этого исходного кода, назвать ее, скажем, VovandroJDK, и начать ее дистрибуцию. Но наш дистрибутив не будет сертифицирован, чтобы можно было называть этот дистрибутив совместимым с Java SE. Вот почему на практике есть несколько поставщиков, которые фактически создают эти сборки, сертифицируют (см. TCK) и затем распространяют их. https://en.wikipedia.org/wiki/Technology_Compatibility_Kit .И хотя поставщики не могут, скажем, удалить метод из класса String перед созданием новой сборки Java, они могут добавить брэндинг (вay!) или добавить некоторые другие утилиты, которые они считают полезными. Но в остальном исходный код одинаков для всех дистрибутивов Java. Один из поставщиков, который создает Java из исходного кода, — это Oracle. Это приводит к двум разным дистрибутивам Java, что поначалу может быть очень запутанными. Сборки OpenJDK от Oracle (!). Эти сборки бесплатны и не имеют торговой марки, но Oracle не выпустит обновления для более старых версий, скажем, Java 13, как только выйдет Java 14. https://jdk.java.net/ OracleJDK — это коммерческая сборка под брендом, выпускаемая начиная с изменения лицензии в 2019 году. Это означает, что ее можно использовать бесплатно во время разработки, но вы должны платить Oracle, если используете ее в рабочей среде. При этом вы получаете более длительную поддержку, то есть все обновления дистрибутива и номер телефона, по которому можно позвонить, если с вашей JVM будут проблемы. https://www.oracle.com/technetwork/java/javase/downloads/index.html .В итоге использование данных версий сводится к тому, что вам требуется платная коммерческая поддержка для используемой версии Java. AdoptOpenJDK - как альтернатива рабочего дистрибутива для работы. В 2017 году группа участников, разработчиков и поставщиков Java User Group (Amazon, Microsoft, Pivotal, Red Hat и другие) создала сообщество под названием AdoptOpenJDK https://adoptium.net/temurin/releases/?version=11%C2%A0 . Они предоставляют бесплатные надежные сборки OpenJDK с более длительной доступностью / обновлениями и даже предлагают вам выбор из двух разных виртуальных машин Java. Для данного курса рекомендуется скачивать версии этой сборки. Если у вас нет особых требований, найдите файл jdk.zip (.tar.gz/.msi/.pkg) по адресу ADOPTIUM и выберите пакет для своей ОС. https://adoptium.net/temurin/releases/?version=11%C2%A0 .
Как нам установить Java или JDK? На данный момент не обращайте внимания на образы Java-Docker, оболочки MSI или пакеты для конкретной платформы. В конце концов, Java — это просто файл .zip; ни больше ни меньше. Поэтому все, что вам нужно сделать, чтобы установить Java на свой компьютер, — это разархивировать файл jdk-{5-13}.zip. Вам даже не нужны права администратора для этого! Все необходимые утилиты как javac, java и многие другие находятся в папке bin. Для примеров используется JDK версии 11. Поэтому все, что вам нужно сделать, это скачать нужный дистрибутив к себе в любое место, разархивировать и поместить каталог /bin в переменную PATH, чтобы вы могли вызывать команду java из любого места. При редактировании Path соблюдайте осторожность — эта переменная содержит важную информацию! Ничего не удаляйте из нее без ясного понимания, для чего вы это делаете. В случае, если вам интересно, установщики с графическим интерфейсом, такие как Oracle или AdoptOpenJDK, выполняет распаковку и изменение переменной PATH вместо вас. После того как вы добавили к переменной Path путь к папке /bin нашего JDK, можно попробовать запустить проверочные команды в консоли из любого места. Команды для запуска:
C:\>java -version
C:\>javac -version
-------------------------- Утилиты:
javac			утилита компилятора java compiler. Переводит исходный код файл(а) *.java в байт-код файл(Ы) *.class. Она компилирует java-файлы в .class-файлы, содержащие байт-код. 
java			утилита нужна для запуска JVM с полученными .class-файлами. Стоит отметить, что JVM является частью JDK. Существуют три версии платформы Java:
1/	Java SE (Java Platform, Standard Edition). 
Используя Java SE, вы можете создавать и развертывать Java-приложения для настольных компьютеров и серверов, а также разрабатывать встроенное программное обеспечение и программы для систем реального времени. Это та часть Java, которую необходимо хорошо знать каждому программисту. Это основа языка. 
2/	Java EE (Java Platform, Enterprise Edition). 
Эта корпоративная версия платформы помогает разработчикам создавать и развертывать переносимые, надежные,  масштабируемые и безопасные серверные приложения на Java. Развивая возможности Java SE, Java EE предоставляет API Web-сервисов, компонентных моделей, удаленного взаимодействия и управления для реализации корпоративного программного обеспечения по принципам SOA и Web 2.0.
3/	Java ME (Java Platform, Micro Edition). 
Java ME предоставляет среду для выполнения приложений, созданных для широкого круга мобильных и встроенных систем, например, мобильных телефонов, карманных компьютеров, телевизионных приставок и принтеров.
Последние две версии (2) и (3) — это очень специфические инструменты, которые нам в этом курсе не потребуются. Нас с вами интересует Java SE (Java Standard Edition, стандартная Java или Java Core). Это та часть Java, которую необходимо хорошо знать каждому программисту. Это основа языка. 
-------------------------- Java Archive Tool:
JAR			утилита, котороя входит в JDK и собирает скомпилированные файлы байт-код *.class, в архив .jar;
.jar			архив, без сжатия.
-------------------------- manifest:
manifest		файл, который должен содержаться в JAR архиве. Текстовый файл, который содержит информацию о:
			Main-Class	класс с методом main;
			Class-Path	список необходимый JAR-ов, через пробел.
jar cfm library.jar manifest.mf -C classes_dir .


========================== Java Basic. Синтаксис, структура кода, классы, методы, конструкторы, точка входа программы:
Java			--ориентирована на кодировку Unicode;
Java			--чувствителен к регистру (class/Class воспринимаются компилятором по-разному);
.java			--Исходный код разделяется на: Пробелы/Комментарии/Лексемы.
//			--комментарий однострочный. До конца строки;
/* ... */		--комментарий многострочный, т.е. все символя между /* и */ игнорируются;
/** ... */		--комментарий документирования. На их этого основе утилита javadoc может формировать набор html страниц. Для разработчиков и поддержки.
ЛЕКСЕМА/token		--в программировании — это минимальная значимая единица языка программирования, которая состоит только из разрешённых символов алфавита языка и не содержит в своём составе другие структурные единицы языка или символы.
Лексемы /tokens		--это обозначения элементарных сущностей, которые нельзя разделить на более мелкие части. Виды. Некоторые примеры лексем в программировании:
Ключевые/Служебные СЛОВА--зарезервированные слова языка программирования, на основе которого строятся конструкции языка, такие как if, while, return, class, public, int, this, void, synchronized, - которые имеют специальное значение и не могут использоваться в качестве идентификаторов;
ИДЕНТИФИКАТОРЫ		--имена переменных, методов, функций, классов и других пользовательских объектов, например: x, myFunction, Person. Имена различных элементов языка, для упрощения доступа к ним. Состоят из букв и цифр, знаков _ и $. Не допскается совпадения с Ключевыми/Служебными словами, Литералами и не может начинаться с цифры. Длина имени не граничена ;
ОПЕРАТОРЫ		--символы, которые выполняют операции над данными, например, +, -, *, =;
ЛИТЕРАЛЫ		--константные значения, фиксированное значение, которое напрямую записывается в исходный код, такие как: числа (42, 3.14); строки («hello»); булевы значения (true, false); символьный литерал в '', типа 'a' '\n', '\\', '\377'; строковый литерал в "", типа: "это строковый литерал" - это константа без имени, явно указанная в тексте программы;
РАЗДЕЛИТЕЛИ		--символы, которые разделяют лексемы, например, точка с запятой (;), запятая (,), скобки ((), {}).
-------------------------- Java class (поля, методы, объекты):
является минимальной, самостоятельной единицей. Всё и всегда пишется в классах, и: поля, и методы, и вложенные классы и интерфейсы.
ПОЛЕ---------------------переменная, принадлежащая к классу, объявляется внутри класса и имеет синтаксис:
[модификаторы доступа] <тип> {<имя> [= <инициализирующее выражение>]};
[]	[модификаторы] и [= <инициализирующее выражение>]	--не обязательны;
<>								--обязательны;
{}								--являются ограничителями, показывающими, где начинается и заканчивается класс. Все, что находится внутри этих скобок, называется телом класса.
()								--являются оператором вызова
[= <инициализирующее выражение>]				--может быть вычисляемым;
public class Student {  				// Поля класса  
    String name; 					// имя студента, с типом String
    int age = 35;					// возраст, с типом int
    double sum = 2.5 + 3.7;				// sum, с типом double 
    double averageGrade = sum + 2 * Math.sqrt(2);	// средний балл, с типом double  
    boolean isEnrolled; 				// статус зачисления, с типом boolean  
}  
*-если поле явно не инициализируется, ему присваевается значение по-умолчанию (0, false, null)
После создания объекта доступ к его полям осуществляется с помощью оператора точки (. ). Например: 
Person person = new Person(); person.name = "John";	// установка значения поля.
System.out.println(person.name); 			// получение значения поля.
МЕТОД---------------------это именованный блок кода, который выполняет определённое действие и может быть вызван по имени. Методы всегда связаны с классами или объектами, тогда как функции в других языках могут существовать сами по себе.
[модификаторы доступа] <тип> <сигнатура> [throw исключения]{<тело>};
<сигнатура>						//сигнатура метода состоит из имени метода, т.е. идентификатора, и принимаемых в метод параметров;
class MyFirstClass {
    public static void main(String[] args) {		//"public static void main" и (String[] args) - принимаемые параметры. 
        System.out.println("Hello world!");	//тело метода;
    }
}
================================ public static void main(String[] args	- начало программы:
public		Модификатор доступа 		— определяет видимость метода (public, private, protected или пакетный доступ по умолчанию).
static		Модификатор static 		— если присутствует, метод принадлежит классу, а не экземпляру. Уровень класса.
void		Тип возвращаемого значения 	— тип данных, который метод возвращает, или void, если ничего не возвращает.
main		Имя метода 			— идентификатор, по которому метод вызывается (по соглашению начинается с маленькой буквы). 
main		метод класса			- Метод main ОБЯЗАН быть static, потому что JVM должна вызвать "ТОЧКУ ВХОДА", без создания экземпляра класса, к тому же JVM не знает, как создавать объекты. Методы с одинаковыми именами и различными параметрами, - являются перегруженными. 
main		метод класса			- не ОБЪЕКТ, потому что на момент старта программы ещё не существует ни одного экземпляра.
	-На время выполнения/вызова метода, управление метода, исполнение приложения, передаётся в тело метода;
	-Возвращается "одно" значение простого или объективного типа: return, someValue. Два или три значения - не может быть;
	-Наличие круглый скобок, при вызове метода, обязательно, так как скобки являются оператором вызова метода.
МЕТОДы и ПОЛя	относятся к классу, то есть, относятся к описанию некоторого объекта. 
ПОЛЯ-----------------------к которым обращается main, также, static. Нестатические поля - требуют класса;
ОБЪЕКТ---------------------должен создаваться. new ключевое слово для создания ОБЪЕКТА. Не статик (Не-static);
ОБЪЕКТ---------------------это конкретный экземпляр класса, созданный в памяти программы. Объект создаётся в heap, у объекта нет имени; 
ССЫЛКА---------------------это адрес объекта в памяти;
КЛАСС----------------------это шаблон, в котором описано, какими свойствами и действиями должен обладать объект. 
ОБЪЕКТ---------------------создаётся при помощи оператора new и вызова конструктора. 
new------------------------возвращает ссылку на объект, выделяет память:
	Body sun;					//объявление ссылки на объект;
	sun = new Body();				//отражение, что переменной sun присваевается ссылка на создаваемый объект, класса Body;
	sun.idNum = Body.nextID++;			//через . происходит обращение к полям класса и присвоение им значений.
	sun.name = "Sun";
	sun.orbits = null;				//присвоение ссылочного литерала null, что означает, что объекта по этой ссылки пока ещё нет, но он будет создан попозже.
---------------
	Car myCar = new Car() 				//создание объекта класса Car.
	Car anotherCar = new Car("Toyota", "Corolla", 2021)	//создание объекта с параметрами.
КЛАСС----------------------это шаблон, определяющий структуру и поведение создаваемых объектов. Он описывает, какие данные будут храниться (поля) и какие операции можно выполнять (методы). По сути, класс — это цифровой чертёж объекта реального мира. 
КОНСТРУКТОР----------------это специальный метод, который инициализирует объект: Предназначен для формирования начального сосотояния объекта - присваивает значения его параметрам (полям) и выполняет начальные действия для подготовки объекта к работе. Имя конструктора совпадает с именем класса. Правила написания конструктора сходны с правилами написания метода:
	-имеет то же имя, что и класс;
	-может принимать параметры для инициализации полей; 
	-память для объекта выделяет оператор new. Вызывается автоматически при создании объекта, через new. new ключевое слово для создания ОБЪЕКТА;
	-не имеет возвращаемого типа (void, int - ничего не пишем);
	-оператор возврата return, прекращает выполнение текущего метода, в котором находимся, а не конструктора в целом.;
	-могут быть перегружены;
	-могут вызывать друг друга, с помощию ключевого слова this, в первой строке конструктора;
	-описывается в классе;
	-если не описан, то создаётся "конструктор default";
	-если в классе описан хотя бы один конструктор, то "конструктор default" - не создаётся;
	-"default конструктор" - это конструктор не имеющий параметров:
class Card {						//"default конструктор" - это конструктор не имеющий параметров
	private long id;
	private String cardHolderName;
	
		public Card() {				//конструктор
			this.id = nxtID++;
	}
		public Card(long id, String name) {	//конструктор
			this.id = id;
			this.cardHolderName = name;
	}
private static long nextID = 0;
}
ДЕСТРУКТОР-----------------выполняют действия обратные конструктору. в Java деструкторов нет, вместо них применяется механизм "сборки мусора" Garbage collector, на случай нехватки памяти для очередного объекта. Garbage collector управляется JVM и в этот процесс лучше не вмешиваться.
[МОДИФИКАТОРЫ ДОСТУПА]:
private				--доступ к (полю, методу, объекту), в контексте класса. Снаружи объект не виден;
package 			--default, none. Явно прописывать не нужно. Доступ для класса и классов в том же пакете;
protected			--доступ к (полю, методу, объекту) самого класса, классов наследников и пакетов;
public				--доступ для всех.
ТОЧКА ВХОДА----------------метод с жёстко заданной сигнатурой "public static void main(String[] args) {" 
public static void main(String[] args) {		//Этот метод - статический, публичный, с одним параметром и аргументами и, без возвращаемого значания.
class Echo {						//класс может называться как угодно
	public static void main(String[] args) {	//метод - жёстко задан
		...
	}
}
-------------------------- ПРАВИЛА НАИМЕНОВАНИЯ:
Пакеты:							java.lang, javax.swing, ru.vtb.learn.java - с маленькой буквы.
Типы:							Student, Runnable, Serializable - с Большой буквы (Классы, Интерфейсы)
Поля, локальные переменные, методы:			value, enabled, distanceFromShop  - с маленькой буквы.
оля-константы:						PI, SIZE_MIN, SIZE_DEF
-------------------------- Понятие типа данных. Размер, диапазон значений:
В связи с тем, что Java относится к категории строго типизированных языков программирования, типы данных имеют в нем очень большое значение. В процессе
компиляции проверяются типы операндов во всех операциях. И если в программе встречаются недопустимые операции, то ее исходный код не преобразуется в
байт-код. Контроль типов позволяет сократить количество ошибок и повысить надежность программы. В отличие от других языков программирования, где допускается не указывать типы данных, хранящихся в переменных, в Java все переменные, выражения и значения строго контролируются на соответствие типов данных. Более того, тип переменной определяет, какие именно операции могут быть выполнены над ней. Встроенные типы данных в Java разделяются на две категории:
1.    объектно-ориентированные, определяются в КЛАССАХ;
2.    необъектно-ориентированные.
ВАЖНО!
В основу языка Java положено восемь элементарных типов данных (их также называют примитивными или простыми). Термин элементарные указывает на то, что эти типы данных являются не объектами, а обычными двоичными значениями. Такие типы данных предусмотрены в языке для того, чтобы увеличить эффективность работы программ. Все остальные типы данных Java образуются на основе элементарных типов. В Java четко определены области действия элементарных типов и диапазон допустимых значений для них. Эти правила должны соблюдаться при создании всех виртуальных машин. Поскольку программы на Java должны быть переносимыми, точное следование этим правилам является одним из основных требований языка.
-------------------------- Элементарные типы можно разделить на четыре группы:
1)      Целочисленные. Эта группа включает в себя типы byte, short, int и long, которые представляют точные целые числа со знаком.
2)      Числа с плавающей точкой. Эта группа включает в себя типы float и double, которые представляют числа, определенные с точностью до определенного десятичного знака.
3)      Символы. Эта группа включает в себя тип char, которая представляет символы символьного набора, такие как буквы и цифры.
4)      Булевские значения. Эта группа включает в себя тип boolean — специальный тип, предназначенный для представления значений типа
истинно/ложно.
-------------------------- Примитивные типы данных Java:
Существует восемь примитивных типов данных в Java:
numeric types					Числовые данные
byte						Целые типы (integral types)
short						Целые типы (integral types)
int						Целые типы (integral types)
long						Целые типы (integral types)
float						Вещественные типы (floating-point types)	
double						Вещественные типы (floating-point types)
char						Целые типы (integral types)	
boolean						Логический тип данных
Примитивные типы хранят значение.
-------------------------- Ссылочные типы данных Java:
Ссылочные типы хранят ссылку на объект, или же тип данных null, то есть нулевую (пустую) ссылку. Существует четыре типа ссылочных данных в Java:
class types					Классы
interface types					Интерфейсы
type variables					Переменные типов
array types					Массивы
-------------------------- Целый тип данных. byte, short, int, long, char:
BYTE	 	-Наименьший по размеру целочисленный тип. Это 8-битный тип со знаком с областью допустимых значений от -128 до 127. ! Переменные типа byte особенно полезны при работе с потоком данных, поступающих из сети или файла. Они полезны также при манипулировании необработанными двоичными данными, которые могут не быть непосредственно совместимыми с другими встроенными типами Java. Для объявления переменных типа byte служит
ключевое слово byte. Например, в следующей строке объявлены две переменных типа byte, названные b и с:                                                                            byte b, с;
SHORT	 	-16-битный тип со знаком. Он имеет область допустимых значений от -32768 до 32767. Вероятно, этот тип используется в Java наименее часто. Ниже приведено несколько примеров объявления переменных типа short:
				short s;
				short t;
INT	 	-Наиболее часто используемым целочисленным типом является int. Это 32-битный тип со знаком, который имеет область допустимых значений от -2147483648 до 2147483647. Кроме других применений переменные типа intчасто применяются для управления циклами и индексирования массивов. Хотя на первый взгляд может показаться, что использование типов byte или short эффективнее использования типа int в ситуациях, когда не требуется более широкий допустимый диапазон значений, предоставляемый последним, в действительности это не всегда так. Это обусловлено тем, что при указании значений типа byte и short в выражениях их тип повышается до int при вычислении выражения. (Повышение типа из byte и short до int описано позже) Поэтому часто тип int наиболее подходит для работы с целочисленными значениями.
LONG	 	-64-битный тип со знаком, удобный в тех ситуациях, когда длина типа int недостаточна для хранения требуемого значения. Область допустимых значений типа longдостаточно великаот -9,223,372,036,854,775,808 до 9,223,372,036,854,775,807. Это делает его удобным для работы с большими целыми числами. 
CHAR	 	-в Java для хранения символов используется тип данных char. Однако программистам на C/C++ следует помнить, что тип char в Java не эквивалентен типу char в С или С++. В C/C++ char — это целочисленный тип, имеющий ширину 8 битов. В Java это не так. Вместо этого в нем для представления символов используется Unicode. Таким образом, в Java тип char является 16-битным. Диапазон допустимых значений этого типа —
от 0 до 65536. Не существует отрицательных значений типа char. Использование переменных типа char демонстрирует следующая программа:
/** 
 * Демонстрация использования типа данных char
 */
public class CharDemo {
    public static void main(String... args) {
        char chl, ch2;
        chl = 88; 					// код переменной X
        ch2 = 'Y';
        System.out.print("chl и ch2: ");
        System.out.println(chl + " " + ch2);
    }
}
Эта программа отображает следующий вывод:
				chl и ch2: X Y
Переменной chl присвоено значение 88, являющееся значением ASCII (и Unicode), которое соответствует букве X. Как уже было сказано, набор символов ASCII занимает первые 127 значений набора символов Unicode. Поэтому, все «старые трюки», применяемые при работе с символами в других языках, будут работать и в среде Java.
ВАЖНО!
Хотя тип char был разработан для хранения символов Unicode, его можно считать также целочисленным типом, пригодным для выполнения арифметических
операций. Рассмотрим следующую программу:
/**
 * Символьные переменные ведут себя подобно целочисленным значениям
 */
public class CharDemo {
    public static void main(String... args) {		//ТОЧКА ВХОДА
        char chl;
        chl = 'X';
        System.out.println("chl содержит " + chl);
        chl++; 						// увеличение значения chl на единицу
        System.out.println("chl теперь " + chl);
    }
}
Эта программа генерирует следующий вывод:
				chl содержит X
				chl теперь Y
-------------------------- Типы данных для работы с дробными числами:
Числа с плавающей точкой, называемые также действительными числами, используются при вычислении выражений, которые требуют получение результата с точностью до определенного десятичного знака. Существуют два вида типов с плавающей точкой: float и double, которые соответственно представляют числа одинарной и двойной точности. см. "long.тип данных.png"
FLOAT	 	-определяет значение одинарной точности, которое при хранении занимает 32 бита. В некоторых процессорах обработка значений одинарной точности выполняется быстрее и требует в два раза меньше памяти, чем обработка значений двойной точности, но в тех случаях, когда значения либо очень велики, либо очень малы, точность вычислений оказывается недостаточной.
Ниже приведен пример объявлений переменных типа float:
	                         float hightemp, lowtemp;
DOUBLE		-двойная точность, как следует из ключевого слова double (двойная), требует использования 64 битов для хранения значений.
В действительности в некоторых современных процессорах, которые оптимизированы для выполнения математических вычислений с высокой скоростью, обработка значений двойной точности осуществляется быстрее, чем обработка значений одинарной точности.
-------------------------- Логический тип данных:
BOOLEAN 	-Java содержит элементарный тип, названный boolean, который предназначен для хранения логических значений. Переменные этого типа могут принимать только одно из двух возможных значений: true (истинно) или false (ложно). Этот тип возвращается всеми операциями сравнения, подобными а < b. Тип boolean обязателен для использования также в условных выражениях, которые управляют такими управляющими операторами, как if и for.


========================== Задание 2:
-------------------------- 1. Создаём у себя на компьютере в любой рабочей папке файл c именем MyFirstClass и расширением .java:
(как пример с:\app\mydir) 
(MyFirstClass.java)
Файл должен содержать в себе текст (исходный код) указанные ниже:
public class MyFirstClass {
    								// тело класса
}
https://davtb-teachbase.api.eric.s3storage.ru/material_images/d1aec71992255683bf516020477ba4d506200623.png
ВАЖНО! Стоит помнить, что имя файла должно в точности повторять, включая первую большую букву, имя класса, описанного в нем. В нашем случае и класс и файл имеют имя MyFirstClass. Это очень удобно, потому что не нужно открывать файл, чтобы узнать, какой класс в нем описан. Достаточно посмотреть на имя файла. Данному соглашению следуют все разработчики в мире разработки Java.
Разберем написанный код:
В первой строке мы сообщаем 'public class', что здесь начинается программа или, если говорить в терминах Java, объявляется класс.
public 			-Модификатор доступа сообщает о том, что метод является публичным, общедоступным.
class			-Чтобы было проще понять, что такое класс, его можно воспринимать как способ организации или оформления кода в файле, некоторое описание. 
{}			-тело класса. В строке 1 и 3 используются фигурные скобки, открывающаяся { и закрывающаяся }, идущие всегда парой. Они являются ограничителями, показывающими, где начинается и заканчивается класс. Все, что находится внутри этих скобок, называется телом класса.
Далее нам необходимо выполнить его компиляцию с помощью компилятора javac:
C:\app\mydir>javac MyFirstClass.java
https://davtb-teachbase.api.eric.s3storage.ru/material_images/1b7447bf25362fc5de98e3bb77cef7b0f86e98ea.png
ВНИМАНИЕ! Обратите внимание на то, что на вход для компилятора необходимо подавать имя файла с расширением (как показано на рисунке выше).
ВНИМАНИЕ! Также обратите внимание на то, что в операционных системах, в которых имена файлов чувствительны к регистру, имена файлов для компиляции и запуска следует указывать с учетом регистра. При этом расширение файла с исходным кодом должно быть «.java» (маленькими буквами).
ВАЖНО! Программа будет работать даже, если имя файла написать с маленькой буквы. Но все разработчики Java следуют соглашению, что имя файла должно начинаться с большой буквы. Привыкайте к таким правилам и тоже следуйте им. В результате выполнения команды рядом появится новый файл с расширением .class (MyFirstClass.class)
https://davtb-teachbase.api.eric.s3storage.ru/material_images/0bb7954f0b6ebd42757956b83f1d2749c83ce430.png
-------------------------- 2. После того как мы получили откомпилированный класс:
запустим его на выполнение с помощью команды: C:\app\mydir>java MyFirstClass
ВНИМАНИЕ! Обратите внимание на то, что на вход для JVM необходимо подавать имя файла без расширения.
OOPS! https://davtb-teachbase.api.eric.s3storage.ru/material_images/912fbf89c2c9f896807f64dcf6274ad656d228df.png
В результате мы видим, что JVM не смогла запустить программу т.к. не нашла метод main (с маленькой буквы). О чем и свидетельствует ошибка 
Error: main method not found in class MyFirstClass
Итак, границы программы заданы, класс объявлен, чего же не хватает? Далее нам необходимо реализовать в теле класса хоть какую-то функциональность. Для этого нужно объявить (создать) точку входа - метод main(). Добавим в тело нашего класса следующие строки кода:
class MyFirstClass {
    public static void main(String[] args) {			//ТОЧКА ВХОДА
        System.out.println("Hello world!");
    }
}
https://davtb-teachbase.api.eric.s3storage.ru/material_images/99adab51357421493e83293f9f852e882293ebc5.png
ВНИМАНИЕ! Метод main(), как и класс, записывается по определенным правилам и тоже содержит ряд ключевых слов и фигурные скобки, обозначающие его границы.
main			--непростой метод, он отвечает за запуск программы. Его внешний вид строго декларирован (утвержден) и прописан в спецификации языка. Данный документ должен быть настольной книгой начинающего разработчика на языке Java. Можно ознакомится на ресурсе https://docs.oracle.com/javase/specs/jls/se11/html/jls-12.html#jls-12.1.4
public static void main(String[] args)
public static void main(String… args)
Разберем написанный нами код:
public 			--Модификатор доступа, так же, как и для класса сообщает о том, что метод является публичным, общедоступным (да, методы тоже можно прятать или оставлять открытыми). В данном случае мы его не скрываем от JVM (Java Virtual Machine — виртуальная машина Java, в которой запускаются и работают Java-программы), чтобы она могла выполнить его независимо от того, где находится класс с этим методом или сама JVM.
static			--Модификатор static позволяет JVM запускать метод main без дополнительных действий с ее стороны.
void			--указывает на тип возвращаемого значения. Каждый метод внутри себя выполняет какую-то деятельность. Результатом этой работы может быть какое-то значение. В нашем случае слово void означает, что метод не возвращает никакого значения, т. е. результатом его работы не является что-то, что мы могли бы использовать за пределами метода main.
main			--имя метода. Позволяет отличать один метод от другого, т.к. класс может содержать огромное их количество. В нашем случае метод называется main, т.е. «главный», «основной». Имена методов в Java принято начинать с маленькой буквы. Имена методов могут состоять из нескольких слов, например, findBook, deleteAllRecords и т. д. В этом случае используются так называемое правило camelCase https://ru.wikipedia.org/wiki/CamelCase, но первое слово в имени начинается с маленькой буквы.
()			--открывающаяся и закрывающаяся круглая скобка. Они выполняют две функции: 
			1/	сообщают, что перед нами метод; а также...
			2/	могут внутри себя содержать описание принимаемых аргументов, так называемая сигнатура метода.
String[] args		--принимаемые методом аргументы. В данном случае метод main принимает в качестве аргумента массив строк, который называется args. При запуске программы мы можем указать необходимые входные параметры к которым в теле метода при необходимости будем иметь доступ (args[0], args[1] … args[n]).
{}			--открывающаяся и закрывающаяся фигурная скобка. Задают границы метода. Все, что находится внутри них, называется телом метода.
В строке "System.out.println("let the world wait!");" для вывода текстовой информации на экран. 
System.out.println	--отвечает за вывод текста. Просто запомните, как это пишется.
println()		--Функция класса PrintStream;
out 			--Поле этого типа является статическим полем класса System, поэтому может использоваться без создания объекта типа System;
System			--Класс.
-------------------------- После вставки нового кода (метода main() как точки входа):
C:\app\mydir>	javac	--снова выполним компиляцию нашего файла с помощью компилятора javac
MyFirstClass.java
C:\app\mydir>	java	--выполним нашу первую программу
MyFirstClass
ТАДАМ! https://davtb-teachbase.api.eric.s3storage.ru/material_images/6cb07ad9d4a6dcf3e7533b03bb31347f9d7032db.png
В результате на экране мы видим наш текст, который мы указали в теле метода main.
-------------------------- 3. После того как нам удалось запустить свою первую программу давайте соберем JAR-архив.
JAR-файл 		--это Java-архив (Java ARchive). Это простой архивный файл, сжатый (иногда с нулевой компрессией) по алгоритму zip. Он был создан для удобства распространения программ, написанных на Java. Так как обычная программа содержит сотни, тысячи, а иногда и миллионы файлов. Файл может содержать:
META-INF/MANIFEST.MF	--файл манифеста;
java-файлы		--исходный код;
class-файлы		--файлы, необходимые для работы программы: картинки, файлы с настройками и прочее (ресурсы) электронные подписи, которые позволяют защитить программу от модификации и др. 
Мы все также находимся в своей рабочей папке C:\app\mydir на уровне которой запускаем команду сборки с помощью утилиты jar:
C:\app\mydir>	jar cfe firstprogram.jar MyFirstClass MyFirstClass.class
https://davtb-teachbase.api.eric.s3storage.ru/material_images/76c8f2358a1ed7178b4cd80c4be87e4228a433c2.png
В результате выполнения получим созданный jar-файл, который можно открывать (запускать) на любом компьютере с установленной JRE.
https://davtb-teachbase.api.eric.s3storage.ru/material_images/107ea375dccfdad18e8a3d032a6acfd90c24bf4e.png
Запустить программу при помощи собранного jar-архива можно следующей командой:
C:\app\mydir>	java -classpath firstprogram.jar MyFirstClass
		java -jar firstprogram.jar
https://davtb-teachbase.api.eric.s3storage.ru/material_images/4ad7c4b737723b9e7a719c7f08dfc0f4a6690f46.png
ВНИМАНИЕ! Разбор всех возможных опций запуска утилиты сборки jar-архива, а также выполнение и запуск программ необходимо изучить самостоятельно.
Для ознакомления рекомендуется прочитать следующие ресурсы: 
1. https://docs.oracle.com/en/java/javase/11/tools/jar.html
2. https://docs.oracle.com/javase/tutorial/deployment/jar/build.html

========================== Задание 3:
На прошлых занятиях мы написали свой первый класс с точкой входа – main() метод. Мы изучили сигнатуру метода, а именно:
String[] args 			-- принимаемые методом аргументы. В данном случае метод main принимает в качестве аргумента массив строк, который называется args.
В данном задании вам предстоит использовать входные параметры для нашей программы. Отредактируйте исходный код метода main() в нашем классе MyFirstClass, который создали в предыдущем домашнем задании (Задание 2). Файл должен содержать следующий текст (исходный код):
public class MyFirstClass {
    public static void main(String... args) {
        System.out.println("And let the whole world wait!");
        for (int i = 0; i < args.length; i++) {
            System.out.println(args[i]);
        }
    }
}
В строке 4 объявляется цикл for. О нем мы еще подробнее поговорим далее в уроках курса. Сейчас же достаточно знать, что общая форма традиционного оператора for выглядит следующим образом:
	for (инициализация; условие; повторение)
	{ 
	// тело цикла с некоторой логикой
	}
Далее:
1. Откройте терминал/командную строку в каталоге, где программа хранится локально.
2. Скомпилируйте программу Java для преобразования файла .java в .class;
3. Запустите программу и передайте аргументы после имени файла, разделённые пробелами;
C:\app\mydir>	java MyFirstClass arg1 arg2 arg3 arg4
Аргументы, переданные программе при ее инициализации, хранятся в массиве args. При запуске программы входные параметры передаются через пробел после названия основного класса программы, содержащего main() метод. При этом мы можем легко получить доступ к переданным аргументам в теле метода по индексу в массиве в том порядке, как они были указаны.
ВНИМАНИЕ! Сами аргументы представляют собой массив строк (тип String). Нумерация (индекс) в массиве аргументов начинается с нуля (args[0], args[1] … args[n]).
Мы также можем изменять значения входных аргументов, получив доступ к его значению по i-му индексу и используя оператор присваивания (=). Код ниже изменит некоторые аргументы, переданные через командную строку в Java.
…
args[1] = "Изменили аргумент командной строки 1";
args[3] = "Изменили аргумент командной строки 3";
Попробуйте самостоятельно запустить выполнение программы с данными изменениями и проанализировать результат.
Всё, что мы передаем как аргументы, имеет тип String. Хотя мы также можем преобразовывать строки в разные типы данных, но преобразование типов будем рассматривать на следующих занятиях. Краткий итог о входных параметрах через main метод:·        
	-Аргументы командной строки Java - это способ ввода данных в программу через командную строку;
	-Аргументы командной строки передаются при выполнении команды запуска сразу после имени файла и разделяются пробелами;
	-Аргументы командной строки принимаются массивом строк ( args[] ) в основной функции;
	-Мы можем получить доступ к аргументам командной строки или изменить их, используя args[i] и операторы присваивания.


========================== Задание 4:
В данном задании мы объявим новый класс, будем создавать экземпляр этого класса, вызывать методы из других мест нашей программы. На практике увидим, как пользоваться конструкторами класса и для чего нужны так называемые Getter and Setter методы.
Для начала нам необходимо создать новый файл с расширением .java и пусть он будет называться MySecondClass. Данный файл должен быть расположен рядом с нашим основным классом, т.е. в той же директории. Далее добавим описание второго класса MySecondClass, реализующего необходимую нам логику согласно заданию выше. ВНИМАНИЕ! Ниже показан только фрагмент кода. Все недостающие строки необходимо написать самостоятельно. Значение … (троеточие) обозначает только то, что был пропущен фрагмент кода и сделано это исключительно в учебных целях.
1	public class MySecondClass {
2	   	 private int a;
3	    	...
4	    	public MySecondClass(int a, int b) {
5	        	this.a = a;
6	        	this.b = b;
7	    		}    
8	    	

9		public void setA(int a) {
10			this.a = a;
11   			}
12	    	...
13		public int getB() {
14			return b;
15    			}
16	     ...
17		public int multi() {
18	       		return this.a * this.b;
19	    	}
20	}
Проведем краткий разбор некоторых строк кода:
Во 2-второй строке			объявляется переменная (поле) класса с типом int и приватным доступом (private).
В строках 4 - 7 мы			объявили свой конструктор класса, который на вход принимает два параметра, а в теле происходит присвоение значений переданных параметров к полям класса.
В строке 9 				объявлен так называемый метод Setter для установки значения полю класса, а в...
13 строчке 				метод Getter уже для получения значений из этого поля. ВНИМАНИЕ! Принято для таких методов устанавливать модификатор доступа public, что позволяет нам вызывать их у созданных экземпляров класса. О назначении этих методов поговорим чуть далее.
В строке номер 17 			у нас объявлен метод multi() и по его названию можно догадаться, что он должен выполняет операцию умножения с числами. 
ВАЖНО! В Java 
Getter and Setter			методы для доступа к private полям класса. 		
Getter and Setter			это два обычных метода, которые используются для получения значения поля класса или его изменения. Так для чего они нужны, если мы можем объявить переменную как public и напрямую через точку получать и менять значения? Это не совсем верный подход, так как тем самым при создании класса мы откроем его данные. Нам нужно как-то защитить наши данные от некорректного вмешательства извне. Поэтому принято, что все поля класса, отвечающие за его состояние и поведение, - закрывать для доступа из вне (помечать как private). Теперь поля вроде как защищены, но получается, что доступ к ним закрыт «намертво». Нет возможности обращаться напрямую из других классов: в программе нельзя даже получить и установить нужного значения, если это понадобится. Это тоже не вариант: в таком виде наш класс практически нельзя использовать. В идеале нам нужно позволить какой-то ограниченный доступ к данным:
	-Другие программисты должны иметь возможность создавать объекты класса;
	-У них должна быть возможность считывать данные из уже существующих объектов;
	-Также должна быть возможность присваивать значения полей, но при этом — только корректные значения. От неправильных наши объекты должны быть защищены;
Все эти требования легко достигаются как раз с помощью специальных методов — геттеров и сеттеров. Эти методы нужно создавать всегда. Даже если в твоих полях нет ограничений на возможные значения и требуется только установить значение переменных, вреда от них не будет.
Setter					метод для установки значения поля класса;
Getter					метод для получения значения поля класса;
Итак, мы описали с вами новый класс, проверили что он у нас успешно компилируется. Теперь приступим к написанию кода с использованием всей функциональности нашего класса.
Для этого изменим код нашего main метода добавив такие строки:
	public class MyFirstClass {
	    public static void main(String... args) {
	...
	MySecondClass secondClass = new MySecondClass();
	secondClass.setA(3); 
	secondClass.setB(3);
	System.out.println("#1 get & set = " + secondClass.multi());
	...
	    }
	}
Далее:
1. Выполните компиляцию нашего нового класса MySecondClass для преобразования файла .java в .class;
2. Выполните также компиляцию основного класса так как в нем поменялась логика;
3. Запустите программу в консоли;
C:\app\mydir>	java MyFirstClass
Проверяем, что полученный результат соответствует написанному коду!
/cors/material_images/82cba6cd08bde6106241575b67c4364e7772fae5.png
ВНИМАНИЕ! Шаги 4 и 5 из задания выполните самостоятельно. Все необходимые знания для этого уже у вас имеются.


========================== Задание 5 (директивы package и import):
В предыдущем задании мы разобрали пошагово необходимые действия для создание нового класса и использовали в примере его функциональность. Как вы помните мы целенаправленно создавали файл с исходным кодом рядом с нашим основным классом. Теперь же давайте возьмем наш файл MySecondClass.java и переместим в другое место. Для этого необходимо в той же директории, где расположены наши классы создать цепочку вложенных директорий, (нижний регистр): 
src
-ru
–- inno
-–- lesson
package 				--–это механизм Java (Пакеты) для группировки классов, которые связаны друг с другом, в одну и ту же «группу» (пакет). Когда проект становится больше, например, приложение или API, полезно разделить код на несколько классов, а классы – на несколько пакетов. Тогда становится легче выяснить, где находится определенный класс, который вы ищете. Организует код логически. 
package					---помогает организовать доступ класса к пакету. Пакет - это ТОЧКА ВХОДА программы. Помогает избегать конфликты имён. ВАЖНО! Структура пакетов в точности отображает структуру файловой системы. Все файлы с исходными кодами (.java) и байт-кодами (расширением .class), образующие один пакет, хранятся в одном каталоге файловой системы. Подпакеты образуют подкаталоги этого каталога. Каждый пакет создает единое пространство имен namespace. Это означает, что все имена классов и интерфейсов в конкретном пакете (папке) должны быть уникальны. Имена в разных
пакетах могут совпадать. Организация классов в виде пакетов позволяет избежать конфликта имен между классами. Чтобы указать, что класс My2Class_4 принадлежит определенному пакету, следует использовать директиву package, после которой указывается наименование (путь) пакета. Данная директива пишется в первой строке класса My2Class_4 и указывает на местоположение точки входа (пакета):
package ru.inno.lesson;
public class MySecondClass {
	...
}					---В итоге в рамках одного проекта полное наименование классов состоят из полного пути всего пакета и имени класса. На примере нашего класса его полное имя выглядит следующим образом: ru.vtb.inno.MySecondClass ВНИМАНИЕ! Полное имя класса всегда уникально в рамках одного проекта. Наименование пакета может быть любым, но необходимо соблюдать его уникальность в проекте и разделение имен пакетов происходит с помощью точки. Соглашение «Code Conventions» рекомендует записывать имена пакетов строчными буквами. Тогда они не будут совпадать с именами классов, которые, по соглашению, начинаются с прописной буквы.
public class MyFirstClass {
    public static void main(String... args) {
    ru.inno.lesson.MySecondClass secondClass = new ru.inno.lesson.MySecondClass();
    secondClass.setA(3);
     secondClass.setB(3);
     System.out.println("#1 get & set = " + secondClass.multi());
    }
}
Итак, мы добавили структуру пакета, в котором находится наш класс, но ошибка так и не ушла? В этом можно еще раз убедиться запустив повторно компиляцию наших классов. Давайте применим выше сказанное и немного отредактируем наш класс MyFirstClass указав полный путь в строке 4 следующим образом:
public class MyFirstClass {
    public static void main(String... args) {
    ru.inno.lesson.MySecondClass secondClass = new ru.inno.lesson.MySecondClass();
    secondClass.setA(3);
     secondClass.setB(3);
     System.out.println("#1 get & set = " + secondClass.multi());
    }
}
В результате если все было выполнено верно, то компиляция должна пройти без ошибок. Теперь компилятор javac нашел класс, так как мы указали полный путь его расположения. Полные имена классов обычно либо длинные, либо очень длинные. Каждый раз писать в коде длинное имя, например java.util.ArrayList, очень неудобно. Поэтому в Java добавили возможность «импортировать классы». Вы можете использовать в своем коде короткое имя класса, но сначала вам нужно будет объяснить компилятору, какое именно «полное уникальное имя класса» соответствует короткому имени. Возможно, у вас в проекте несколько классов с таким именем. Чтобы использовать короткое имя класса в своем коде, вам нужно добавить вот такую конструкцию в свой код:
	import полное.уникальное.имя.класса;
	import полное.уникальное.имя.*;	---импорт всех классов;
	import ru.inno.lesson.*;	---импорт всех классов из другого пакета. Если вам нужно использовать много классов из определенного пакета, их импорт по одному приводит к большому количеству операторов импорта. Можно импортировать все классы, используя символ * вместо имени класса;
import					---способ сказать компилятору какой класс использовать и где он находится. Не добавляет код в программу.
Снова перед компиляцией немного изменим код нашего класса с учетом сказанного:
import ru.inno.lesson.MySecondClass;
public class MyFirstClass {
    public static void main(String... args) {

    MySecondClass secondClass = new MySecondClass();
    secondClass.setA(3);
     secondClass.setB(3);
     System.out.println("#1 get & set = " + secondClass.multi());
    }
}
Популярные пакеты Java:
import java.util.*;           		// Коллекции, даты, Scanner
import java.io.*;             		// Работа с файлами
import java.net.*;            		// Сетевое программирование
import java.sql.*;            		// Работа с базами данных
import javax.swing.*;         		// Графический интерфейс
import static java.lang.Math.*;  	// Статический import (для констант). Теперь можно писать PI вместо Math.PI


========================== Типы данных, операторы:
Java			---строго типизированный язык. Тип не может меняться в процессе работы приложения, -тип известен на момент его компиляции. Типы данных: примитивные и ссылочные данные. 
Ссылочные типы данных	---предназначены для работы с объектами. Именуются с большой буквы;
Ссылочные типы данных	---переменные содержат ссылки на объект;
Ссылочные типы данных	---тип переменной определяет контракт доступа к объекту;
Примитивные типы данных	---предназначены для работы со значениями естественных, простых типов;
Примитивные типы данных	---переменные содержат непост=редственно значение;
Характеристики типов данных:
1/	множество значений: 
		для примитиных - это значение из диапозона типа;	
		для ссылочных типов, - это определяется контрактом;
2/	возможные операции со значениями:
		для примитиных - это различные операторы (арифмитические, логические);	
		для ссылочных типов, - это действия, входящие в контракт типа. Вызов метода и обращение к полям;
3/	форма хранения и представления, определяется реализацией JVM:
		для примитиных - ;	
		для ссылочных типов данных- ;
========================== ПРИМИТИВНЫЕ типы данных = простые типы данных:
-------------------------- boolean - логический тип простых данных:
true
false
-------------------------- целочисленные типы простых данных:
"Расширяющийся" список, от char до long:
char		символ Unicode 	16-бит. Не может иметь отрицательных значений.	(от 0 до 65'536) "аналог" short
byte
short				16-бит. со знаком				(от -32'768 до 32'767)	
int
long				64-бит.
"ШИРОКИМ" типом данных, считается тот тип данных, переменные которого могут принимать большее количество значений.
-------------------------- вещественные типы простых данных:
Хранят дробные числа, числа с плавающей точкой. Хранят по специальному стандарту IEEE- 754-1985:
float				32-бит.
double				64-бит. хранит, как целую часть числа, так и вещественную часть числа.
Легальные значения:
Positive infinity (Infinity)
Negative infinity (-Infinity)
Not a Number (NaN)
-------------------------- операторы простых данных:
Имеют приоритет выполнения, как в математике. Операторы, по приоритетам выполнения:
постфиксные/префиксные		Инкремент/Декремент. i++, i-
унарные				+ и -
создание и приведение
арифметика
побитовый сдвиг
сравнение			
равенство
И, and
исключающие ИЛИ, xor
включающие ИЛИ, or
условное И, and
условное ИЛИ, or
условный оператор
операторы присваивания		= постое присвоение. Присваивание с действием: +=, -=, *=, /=, %=, >>=, >>>=, &=, ^=, |=
-------------------------- операторы простых данных, логического типа:
==		равенство;
!=		неравенство;
!		отрицание;
&		логическое сложение "И", and. Вычисление по полной схеме, т.е. результат будет вычисляться;
|		логическое умножение "ИЛИ", or;
^		логическое "исключение или" xor;
&&		условное "И". Вычисление по сокращённой схеме, т.е.;
||		условное "ИЛИ";
-------------------------- арифметические операторы простых числовых типов данных:
+
-
*
/
%		остаток от деления первого значения на второе.
Результат имеет тип, совпадающий с "наиболее широким типом" из типов операторов, но не меньше, чем "int". 
увеличивают/уменьшает значение на 1.
Инкремент	Постфиксная форма: 	i++, i--
		int i=0; System.out.println(i++)
		0, i = 1	
Декремент	Префиксная форма:	++i, --i	новое значение.
		int i=0; System.out.println(++i)
		1, i = 1
-------------------------- операторы сравнения простых числовых типов данных:
>и<		строгое сравнение
>=и<=		нестрогое сравнение
==		равенство
!=		неравенство
Результат-логическое сравнение: true/false
Сравнение проводится в "наиболее широком" типе, из типов операндов.
-------------------------- преобразование простых числовых типов данных:
неявное преобразование		преобразование к "более широкому" типу;
явное преобразование		преобразование к указанному типу, с помощью оператора (type)expr, когда, например, нужно приобразовать от "широкого типа" к более "узкому типу".
========================== ССЫЛОЧНЫЕ типы данных. ОЕРАТОРЫ. ПРЕОБРАЗОВАТЕЛИ. ОБЁРТКИ:
-------------------------- операторы:
1/	не применимы арифметические операторы;
new	создание обекта класса. Так вызывается конструктор класса;
=	присвоение ссылки. типы вырадения справа, должен допускать присвоение в тип переменной слева;
== !=	сравение ссылок. Сравнение только ссылок, а не состояние объектов!;
.	разыменование ссылки. Вызвать определённый метод или обратиться к доступному полю, у объекта какого либо класса:
	. reference.method()
	. reference.field
()	вызов метода;
2/	у любого объекта, ссылочного типа, можно вызвать методы, объявленные в классе Object.
-------------------------- классы и обёртки примитивных типов:
"Класс-обёртка" 	-ссылочное представление над примитивными типами, потому как значение примитивных типов не могут быть непосредственно использованы в контексте, где требуется ссылка;
"Класс-обёртка" 	-ссылочное представление значений примитивных типов, что и является основной задачей "классов-оберток";
"Класс-обёртка" 	-хранит значение примитивных типов. Экземпляр такого класса хранит внутри значение примитивного типа и предоставляет доступ к этому значению;
"Класс-обёртка" 	-хранит вспомогательные функции и константы, для работы со значениями примитивных типов.
"Класс-обёртка" 	-имеют публичный, public конструктор;
equals()		-объекты "Классов-обёрток" могут сравниваться между собой методом equals(), который отличается от "==";
<type>Value()		-значение примитивных типов можно получить из объектов, вызовом методов <type>Value();
"Классы-обёртки" 	-предоставляют статические методы работы с примитивными типами.
Каждому примитивному типу сопоставлен соответствующий "Класс-обёртка":
Простой тип	|	Класс
-------------------------------
boolean		|	Boolean
byte		|	Byte
char		|	Character
double		|	Double
float		|	Float
int		|	Integer
long		|	Long
short		|	Short
		|	Number	-общий предок для всех классов обёрток (механизм наследования)
константы типов:	Integer.MAX_VALUE, Double.NaN
конструкторы:		FLoat(float value), Float(String s) по значению и строке;
получение значений:	Boolean.booleanValue(), Float.floatValue() 	методы;
преобразования типов:	Integer.parseInt(String s), Float.byteValue()	методы;
проверка состояния:	compareTo(...), Double.isInfinite()		методы;
специальные методы:	Double.longBitsToDouble(...), Integer.toHexString()
-------------------------- стандарт IEEE 754-1985:
double		числа могут выглядеть по разному, потому как в памяти они имеют значение максимально приближенное к значени;
BigDecimal	класс повышенной точности. Класс для динной арифметики:
BigDecimal	хранит число как есть;
BigDecimal	не ограничен диапозоном double;
BigDecimal	неизменный (Immutable);
BigDecimal	неприменимы арифметические операторы в качестве вычислительных операций, но есть набор методов, которые осуществляют арифметические операции:
		BigDecimal a = BigDecimal.ONE
			.multiply(new BigDecimal("3.1415"))
			.add(new BigDecimal("10"));
			// 1*3.1415+10 - над последовательностью и приоритетом операций программист следит самостоятельно, но зато лишены недостатков приблизительности double.
BigInteger	класс повышенной точности. Класс для динной арифметики. Для вычислений цельночисленных;
-------------------------- преобразование ссылочных типов данных:
преобразования возможны, если типы совместимы между собой:
-наследование:
	-если контракт целевого типа является частью контракта приводимого типа:
		-Родительский тип считается более общим, чем дочерни;
		-Более "широким" является тип, переменные которого могут принимать большее количество значений;
-интерфейс.
неявное преобразование	-преобразование от более "узкому", к более "широкого", Integer в Object - вохможно:
		Integer i = new Integer(5);
		Object o = i;			//Object является родителем всех классов, т.е. более "широким".
		i = (Integer) o;		//Integer - более "узким", в смысле наследования, а не  числовых диапозонов.
явное преобразование	-преобразование от более "широкого", к более "узкому", с помощью оператора явного преобразования (type)expr:
		Integer i = new Integer(5);
		Object o = i;
		if (o instanceof Integer) {
			i = (Integer) o;
		...
		}
		else { ... }		
java.lang.ClassCastException			//ошибка, при невозможности преобразования коассов.
instanceof					//оператор, возвращающий true,-если тип применим к объекту, и false, - если не применим. Оператор не позволяет определить реальный тип объекта, а лишь проверяет его соответствие указанному типу;
========================== ССЫЛОЧНЫЕ типы данных. СТРОКИ. МАССИВЫ. ИНСТРУКЦИИ:
-------------------------- ссылочный тип данных String:
Тип для работы со строками			String	НЕИЗМЕНЯЕМЫЙ ТИП!
значение любого типа, может быть приведено к	String
		String a = "Hello";
		String b = new String(" world!");
Если хотя бы один операндов оператора является ссылкой на строку, типа String, то остальные операнды, также, приводятся к строке, а "оператор трактуется, как конкатенация строк". Это означает, что при использовании этого оператора между двумя строками Java объединяет их в одну строку. 
КОНКАТЕНАЦИЯ 	-операция объединения строк, которая возвращает новую строку, как результат слияния второй строки с окончанием первой. Часто её называют «склеивание строк» или «сложение строк». 
""		-строковый литерал "" - имеет тип String
String		-класс для работы со строками. является неизменяемым объектом (immutable) объект;
		String s = "Hello";
		String s1 = ", world!";
		String s = s + s1;			//создастся новая строка, в начало которой поместиться значение строки s, потом поместиться значение строки s1. Ссылка будет переприсвоена переменной s, старый объект будет утерен. Старый объект потом уберёт Garbage Collector.
значение любого типа можно привести к строке.
StringBuffer	-класс для работы со строками. java.lang.StringBuffer. Изменяемый (mutable) объект;
StringBuilder	-класс для работы со строками. Копия StringBuffer, появился чуть позже StringBuffer. Не является безопасным, с точки зрения многопоточности. Для локальных объектов. Изменяемый (mutable) объект, - это означает, что содержимое последовательности символов, представленной этим классом, можно изменять без создания новых объектов, в отличие от класса String (который является неизменяемым), StringBuilder позволяет динамически изменять содержимое, и не создаёт лишних объектов в памяти.
-------------------------- метод append():
добавляет заданную строку в конец существующей последовательности без создания нового объекта:
		StringBuilder sB = new StringBuilder();
		sb.append("Hello");			//метод присоединения.
		sb.append(", ").append(" world").append("!");
		String helloWorld = sb.toString();	//метод toString приводит к типу String, который можно дальше использовать.
-------------------------- метод equals():
отличается от "==", так как:
== 		сравение ссылок. Сравнение только ссылок, а не состояние объектов! Поэтому может быть непредсказуемый результат:
		String s = "hello";			//будет создан в "pool строк"
		String s1 = new String("hello");	//создан новый объект, поэтому ссылается на другую область памяти;
		String s2 = "hello";			//JVM знает это значение и присваевает ссылку на уже существующую область памяти;
		s == s2					//true
		s == s1					//false	
в Java есть:
"cash чисел"	это механизм, который позволяет повторно использовать объекты Integer для часто используемых значений. Это помогает оптимизировать использование памяти — не создавать новые объекты для часто используемых значений. Улучшить производительность — кэширование позволяет избежать дорогостоящих операций создания объектов. По-умолчанию Java кэширует значения в диапазоне от -128 до 127; 
"pool строк"	это специальная область памяти в Java Heap, где JVM хранит уникальные строковые литералы, также известен как String Constant Pool или String Intern Pool. Благодаря неизменяемости строк Java может безопасно переиспользовать одни и те же строковые объекты для нескольких переменных, если они имеют одинаковое содержимое. 
поэтому, данные типа String, нужно сравнивать через equals(), а не через "=="
-------------------------- ссылочный тип данных. МАССИВ:
МАССИВ		упорядоченный набор элементов одного типа. Элементами могут быть значения "примитивных/простых" и "ссылочных" типов;
МАССИВ		сходны со строками, является неизменяемыми (immutable);
МАССИВ		являются объектами и наследуются от класса Object. Доступ к элементам по целостному индексу, [целочисленный индекс] с помощью оператора [];
[]		оператор индексации массива;
		int[] numbers = {1, 2, 3, 4, 5};	//массив с явным указанием элементов
		int[] nums = new int[3];
		nums[0] = 10;				//элемент индекса: 0
		nums[1] = 20;				//элемент индекса: 1
		nums[2] = 30;				//элемент индекса: 2
		------------------------------------------------------------
	int array1[], justIntVariable = 0;		//объявление массива array1[]
	int[] array2;					//объявление массива [], с переменными, перечисленными через ",";
	array2 = new int[20];				//инициализация массива, состоящего из 20 элементов (от 0 до 19);
	for (int i = 0; i < array2.length; i++)
		array2[i] = 1000;	
-------------------------- ссылочный тип данных. МНОГОМЕРНЫЕ МАССИВЫ:
-состоят из одномерных массивов, элементами которых являются ссылки на массивы меньшей размерности.
-при создании объекта необязательно указывать все размерности;
-массив необязательно должен быть "прямоугольным".
	int[][] twoDimArr = new int[10][5];		//матрица: 10 строк на 5.
	int[][] numbers = { {1, 2}, {3}, {4, 5, 6}};	//явно.
-------------------------- ссылочный тип данных. ИНСТРУКЦИИ:
Выражения присваивания;
Постфиксные/префиксные формы выражения, с операторами инкремент/декремент, типа: i++, i-;
Конструкции вызова методов;
Выражения создания объектов;
Составные инструкции;
Инструкции управляющие порядком вычислений.
-------------------------- СОСТАВНАЯ ИНСТРУКЦИЯ. БЛОК инструкций:
определяет область видимости локальных переменных.
	int a = 5;					//переменная вне инструкции
	int b = 10;
	{						//блок инструкций
	  int c = a + b;				//переменная в блоке инструкции. Не видна за пределпоми блока
	  int d = a - b;				//переменная в блоке инструкции. Не видна за пределпоми блока, тогда как переменные a и b - доступны;
	}						//блок инструкций
a = c + d;						//ошибка, потому как переменная объявленная внутри блока, не видна за её пределами.
БЛОК инструкций			применяется в инструкциях определяющиъ порядок вычисления.
-------------------------- СОСТАВНАЯ ИНСТРУКЦИЯ. ВЕТВЛЕНИЯ - Инструкции управляющие порядком вычислений:
-------------------------- полная форма ВЕТВЛЕНИЯ:
	if (Логическое Выражение)
	  Инструкция_1 или { БлокИнструкций }
	else						//если (Логическое Выражение) false, то исполнение переходит в ветку else:
	  Инструкция_2					//будет относиться к ветвлению. ПЕРВАЯ;
	  Инструкция_3					//не будет относится к ветвлению, но выполнится- не относясь к ветвлени. Чтобы Инструкция_3 относилась к ветвлению, - необходимо описать "БлокИнструкций" с "Инструкция_2" и "Инструкция_3". "БлокИнструкций" - хорошо и правильно использовать даже с одной инструкцией.
-------------------------- НЕполная форма ВЕТВЛЕНИЯ:
	if (Логическое Выражение)			
	  Инструкция_1 или { БлокИнструкций }		//если (Логическое Выражение) false, то Инструкция_1 игнорируются.
-------------------------- оператор ВЕТВЛЕНИЯ:
Тернарный оператор в Java — это компактная альтернатива классической конструкции if-else, которая позволяет записать простое условие в одну строку. Он принимает три операнда — отсюда и название. 
Синтаксис:
	Переменная = 	условие 		? выражение1 	: выражение2. 
	Переменная = 	<логическое выражение> 	? <значение_1> 	: <значение_2> 
Условие 	это логическое выражение, результатом которого будет true или false.
выражение1 	это значение, которое вернётся, если условие истинно, true;
: 		отделяет вторую ветку: выражение2 выполнится, когда условие ложно, false.
*-типы операндов выражение1 и выражение2 должны быть "совместимы".
	double factor = (a > b) ? 1 : 0.7;		//если выражение a > b true, то вернёт 1, если же false, то вернёт 0.7
Особенности:
-Не просто выполняет действие — возвращает значение;
-Подходит для ситуаций, где результат условной проверки нужно сразу присвоить переменной, передать в метод или вывести на экран;
-Можно использовать прямо внутри выражений, что делает код более выразительным и лаконичным.
-------------------------- СОСТАВНАЯ ИНСТРУКЦИЯ. БЛОК переключателей. оператор switch:
это конструкция, которая оценивает значение переменной и выполняет соответствующий блок кода в зависимости от этого значения. Она представляет собой альтернативу длинным цепочкам if-else, делая код более читаемым и часто более производительным, когда больше чем два исхода true/false. Основное назначение switch-case — обработка множественных вариантов значения одной переменной. Это особенно удобно, когда программе нужно выполнить разные действия в зависимости от конкретного состояния или выбора пользователя. 
Синтаксис. Форма записи оператора switch в Java:
	switch (variable) { 
	  case value1: 			// Если переменная равна value1, то выполнится этот код 	
	  break; 
	  case value2:			// Если переменная равна value2, выполнится этот код 	break; ... 	  default:			// Если ни одно из значений не совпало, то выполнится этот код }. 
switch 		-Ключевое слово, сам оператор;
variable 	-Переменная, имя переменной, которое указывают в круглых скобках и сравнивают с блоками кода внутри оператора.
case		-Блоки содержат возможные значения переменной и код, который выполнится в случае совпадения с исходным условием. Количество блоков case может быть неограниченным, а внутри каждого блока могут находиться любые операторы или выражения.
break 		-Оператор останавливает проверку и срабатывает в ситуациях, когда значение переменной совпадает с одним из блоков case. Если не указать break, то программа продолжит анализировать последующие блоки, пока не проверит их все и не достигнет конца.
	switch (Выражение) {
	  case n: <Инструкции>		//описание варианта, при соответствии "Выражение" "case n", выполнение перейдёт дальше к case m;
	  case m: <Инструкции> break;	//break - оборвёт ветвление, останавливает вариан выполнения. Передаёт управление первой инструкции, следующей за блоком переключателей.
	  ...
	  default: <Инструкции>		//если ни один case не подошёл
switch-case				//предпочитают не использовать, а использовать if-else.
switch-case				//для типов char, byte, short, int, String, enum
-------------------------- ссылочный тип данных. ЦИКЛЫ:
-------------------------- условный цикл while:
-------------------------- форма с предусловием:
	while (Логическое Выражение)
	  Инструкция
Выполняется пока условие истинно. Если, при входе в цикл, условие ложно, то цикл не выполняется.
-------------------------- форма с постусловием:
	do
	  Инструкция
	while (Логическое Выражение)
Выполняется пока условие истинно. При первом входе в цикл, проверка условия не производится. Чаще всего не используются.
-------------------------- цикл с предусловием for:
не является циклом со счётчиком:
	for(СекцияИнициализации; ЛогическоеВыражение; СекцияИзменения)
	  Инструкция
Все секции необязательные. Тело может ьыть пустым.
	for( ; ; );			//означает вечный цикл.
СекцияИнициализации		могут быть списком выражений, разделённых запятой. Допустимо объявление переменных в секции инициализации. Инициализация значений различных типов не могут быть внутри заголовка;
СекцияИзменения			могут быть списком выражений, разделённых запятой;
	for (i = 0, j = 50; j >= 0; i++, j--) {
	// ...
	}
---------
	for (int i = 0, j = 50; j >= 0; i++, j--) {
	// ...
	}
---------
	int i; Cell node;		//объявляем до цикла, чтобы использовать внутри заголовка цикла.
	for (i = o, node = head;
	     i < MAX && node != null;
	     i++, node = node.next) {
	/ ...
	}
-------------------------- МЕТКИ. команда break:
*-Обычно не используются. Код можно переписать без меток!
применяется для завершения выполнения кода блока инструкций.
break			безымянная форма, завершение текущего блока;
break метка		именованная форма, завершение указанного блока;
завершить блок, который, сейчас, не выполняется, - НЕЛЬЗЯ!
	privat float[][] matrix;

	public boolean workOnFlag(float flag) {
	  int y, x;
	  boolean found = false;
	  search:						//метка
	    for (y = 0; y , matrix.length; y++) {		//цикл_1
	      for (x = 0; x< matrix[y].length; x++) {		//цикл_2, внутри цикл_1
		if (matrix[y][x] == flag) {
		  found = true;
		  break search;					//метка "наружного" цикла. прервётся блок инструкций "внешнего" цикла. Прерывает итерацию цикла, выходя из цикла переходит дальше по коду.
		}
	      }
	    }
		// ...
	}
-------------------------- МЕТКИ. команда continue:
*-Обычно не используются. Код можно переписать без меток!
применяется для завершения выполнения кода блока инструкций.
break			безымянная форма, завершение текущей итерации;
break метка		именованная форма, завершение итерации указанного цикла;
завершить блок, который, сейчас, не выполняется, - НЕЛЬЗЯ!
	static void doubleUp(int[][] matrix) {
	  int order = matrix.length;
	  column:						//выполнение текущей итерации цикла_1 прервётся, и начнётся следущая итерация этого же цикла. Цикл не завершает работу.
	    for (int i =0; i < order; i++) {
	      for (int j = ; j < order; j++) {
		matrix[i][j] = matrix[j][i] =
		  matrix[i][j] * 2;
		if (i == j)
		  continue column;				//
	       }
	     }
	}
-------------------------- Инструкция return. Возврат из метода:
прекращает выполнение метода и возвращает его результат.
return value;		с возвращаемым значением (литерал, переменная). Значение должно быть приводимо к типу, возвращаемому методом;
return;			без возвращаемого значения. Метод void. Конструкторы.




