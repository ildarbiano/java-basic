Задание 1
Задача состоит в том, чтобы:
Написать на Java метод getStringFromStream(Stream<String>), который превращал бы входящий поток строк стригов в строку, слова в которой нужно разделить пробелом. Порядок слов в строке должен быть равен порядку слов в потоке. Использовать метод collect() у  Stream<String>. В качестве параметра нужно передать коллектор (объект типа Collector<String>). Коллектор можно получить, вызвав метод joining() у класса Collectors.
Подсказка: 
Для реализации можно использовать метод collect() у  Stream<String>. В качестве параметра нужно передать коллектор (объект типа Collector<String>). Коллектор можно получить, вызвав метод joining() у класса Collectors.
public static String getStringFromStream(Stream<String> stringStream) {
        //напишите тут ваш код
        return "";
}

Задание 2
public static void printList(ArrayList<Integer> list) {
        for (int i = 0; i < list.size(); i++) {
            System.out.println(list.get(i));
        }
    }
Перед тобой готовый к использованию метод printList(ArrayList<Integer>), который все элементы списка по порядку выводит в консоль.
Сейчас метод реализован с использованием оператора for.
Задача - переписать реализацию метода printList(ArrayList<Integer>), используя метод списка forEach(), принимающий лямбда-выражение. Логика работы метода остается той же. 
Подсказка: Нужно использовать лямбда-выражение, передавая его в метод forEach()


